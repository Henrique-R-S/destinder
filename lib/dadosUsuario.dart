class Mensagem {
  final String email;
  final String senha;

  Mensagem(this.email, this.senha);
}

class Usuario {
  var icone;
  final String titulo;
  final String subtitulo;

  Usuario(this.icone, this.titulo, this.subtitulo);
}